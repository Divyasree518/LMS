# TODO: Add Return Book System to Student Portal

## Steps
- [x] 1. Update backend `bookController.js` to calculate fine on return (Rs. 1/day overdue)
- [x] 2. Update frontend `api.js` to add `getBorrowedByUser` and pass `borrowRecordId` to `returnBook`
- [x] 3. Update `StudentPortal.js` to add "My Books" page with borrowed books list and return action
- [x] 4. Update `student-portal.css` with styles for borrowed books, overdue status, and fine display
- [x] 5. Implementation complete


